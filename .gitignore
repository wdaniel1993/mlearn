# Private runtime data — never commit
data/
cards/
*.db
*.db-wal
*.db-shm

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node tooling (mermaid parser)
tools/node_modules/

# Secrets
.env

# Machine-specific config (paths, provider) — see config.yaml.example
config.yaml

# Derived seed JSON contains verbatim article excerpts — regenerated, not committed.
seeds/phase1_cards.json

# OS
.DS_Store
# Private deployment notes (never share)
docs/private/

# Debug artifacts
tools/*_spec.txt
tools/screenshots/node_modules/
