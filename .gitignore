# Private runtime data — never commit
data/
cards/
*.db
*.db-wal
*.db-shm

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node tooling (mermaid parser)
tools/node_modules/

# Secrets
.env
config.yaml   # machine-specific (paths, provider), see config.yaml.example

# Derived seed JSON contains verbatim article excerpts — regenerated, not committed.
seeds/phase1_cards.json

# OS
.DS_Store